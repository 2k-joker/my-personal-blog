<div class="sear-area">
  <h2 class='text-center mt-4'>Search Articles</h2>
  <%= form_tag search_article_path, remote: true, method: :get do %>
    <div class="container">
      <div class="p-1 bg-light rounded rounded-pill shadow-sm mb-4">
        <div class="input-group">
          <%= text_field_tag :article, params[:article], placeholder: 'search', autofocus: true, class: 'form-control border-0 bg-light' %>
          <div class="input-group-append">
            <%= button_tag type: :submit, class: 'btn btn-link text-primary' do %>
              <%= fa_icon 'search' %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>

<div id="article-results">

</div>

<%= render 'article' %>

<div class="flickr_pagination mb-4">
  <%= will_paginate @articles, :container => false %>
</div>

  

